# Description: signal processing blocks to implement software radios
# URL: http://gnuradio.org/
# Maintainer: David Brooke, crux at dbrooke dot me dot uk
# Depends on: boost cheetah-template cppunit fftw gsl python-lxml portaudio pygtk python-numpy python-six swig wxpython

name=gnuradio
version=3.7.13.4
release=2
source=(http://gnuradio.org/releases/gnuradio/gnuradio-$version.tar.gz)

build() {
	cd $name-$version

	mkdir build
	cd build
	cmake -DCMAKE_INSTALL_PREFIX=/usr ..
	make
	make DESTDIR=$PKG install
	find $PKG/usr/share -type f -name "README*" -delete
	rm -rf $PKG/usr/libexec
}
